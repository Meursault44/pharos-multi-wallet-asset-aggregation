{
  "name": "pharos-multi-wallet-asset-aggregation",
  "version": "1.0.0",
  "type": "module",
  "description": "Read-only multi-wallet asset aggregation for Pharos AI agents.",
  "main": "scripts/aggregate-assets.ts",
  "bin": {
    "pharos-aggregate-assets": "scripts/aggregate-assets.ts"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "aggregate": "tsx scripts/aggregate-assets.ts",
    "test": "node --test tests/*.test.mjs"
  },
  "keywords": [
    "pharos",
    "agent",
    "skill",
    "wallet",
    "portfolio",
    "aggregation"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.20.6",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18"
  }
}
